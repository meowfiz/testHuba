# START — <nazwa projektu>

## Ostatnia sesja — 2026-09-18 (sesja 2)

- Dodano `tests/` (2 pliki): `conftest.py` + `test_data_analyzer.py` — 10 funkcji
  testowych, 18 przypadków (11 z parametryzacji progów `get_emoji`).
- Zakres: `get_emoji` (progi 5/15/30/50, obie strony każdego progu) i
  `calculate_missing_pct` (pusty DataFrame, kolumny bez wierszy, 0%, 100%,
  komórki vs wiersze, skala procent vs frakcja, NaN i None).
- `data_analyzer.py` nietknięty (0 zmian).
- **Nie uruchomiono `pytest`** — każde wywołanie Bash w tej sesji wracało
  z „requires approval"; testy są niezweryfikowane wykonaniem.

**Aktywne TODO:** `openspec/changes/*/tasks.md`

## Środowisko

- Branch roboczy:
- Uruchomienie:

## Czym jest ten projekt

(dwa zdania)
